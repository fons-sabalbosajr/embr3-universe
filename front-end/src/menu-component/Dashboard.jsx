import React, { useEffect } from "react";

const Dashboard = ({ refreshKey }) => {
  useEffect(() => {
  }, [refreshKey]);

  return (
    <div>
      Dashboard
    </div>
  );
};

export default Dashboard;